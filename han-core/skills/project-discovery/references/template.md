# Project Discovery

- **Last Updated:** {YYYY-MM-DD HH:mm}

## Repository

- Default branch: {branch-name}
- CI/CD: {platform} `{config-path}`
- Containers: `{path}`
- Git hooks: `{hook-config-path}`

## Documentation

- Docs: `{docs-dir}`
- ADRs: `{adr-dir}`
- Coding standards: `{standards-dir}`
- CLAUDE.md: `{path}`
- AGENTS.md: `{path}`
- README: `{path}`

<!-- Repeat the following section for each project in the repository. -->
<!-- For single-project repos, use the repository name as the project name. -->
<!-- Omit any item or sub-section that has no discovered value. -->

## {Project Name}

- Root: `{path}`
- Language: {language} {version}
- Package manager: {package-manager}
- Dependency manifest: `{manifest-path}`
- Lock file: `{lock-file-path}`

### Frameworks and Tooling

- Web: {framework}
- Frontend: {framework}
- Test: {framework}
- ORM/DB: {library}
- Build: {tool} `{config-path}`
- Lint: {tool} `{config-path}`
- Format: {tool} `{config-path}`
- Typecheck: {tool} `{config-path}`
- Task runner: `{path}`

### Commands and Tests

- Install: `{command}`
- Test: `{command}`
- Lint: `{command}`
- Build: `{command}`
- Dev server: `{command}`
- Format: `{command}`
- Test directory: `{path}`
- Test file pattern: `{pattern}`
- Coverage: {tool}

### Configuration

- Environment: `{.env-path}`
- Config: `{config-path}`
