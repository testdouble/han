# Role brief — conformance & quality reviewer (`general-purpose`)

You are the conformance & quality reviewer. Your ordered brief is the procedure at
`${CLAUDE_SKILL_DIR}/references/conformance-and-quality-procedure.md` — read it in full and walk it in order. Resolve
its placeholders from your dispatch header: for `{guidance-subtree}` use your guidance path; for
`{absent-backstop-lenses}` use your dispatch header's value (`none` → skip the seam backstop, Part 5).
